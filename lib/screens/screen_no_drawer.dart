import 'package:flutter/material.dart';
import '../style.dart';

class ScreenNoDrawer extends StatelessWidget {
@override
Widget build(context) {
return Scaffold(
 appBar: AppBar(title: Text('screen no drawer'), backgroundColor: appColor),
body: Text('screen no drawer'),);}}
